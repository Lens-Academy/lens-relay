// CommentThread.tsx was removed in Task 10 (html-editor: mount shared CommentsLayer sidebar).
// The CommentThread popover is replaced by CommentsLayer (shared sidebar).
// Thread editing/reply behaviour is now covered by CommentsLayer and CommentCard tests.
// Integration coverage is deferred to Task 11.
import { describe, it } from 'vitest';

describe.skip('CommentThread (deleted — Task 10)', () => {
  it('stub — functionality moved to CommentsLayer/CommentCard', () => {});
});
